# Ранний август: план задач, не привязанных к датам

Выполняется сразу (до прогона C 11–12.08). После — калибровка v0.
Детализирует недели 0–1 из `docs/plan-2027.md`; парсеры недель 1–8 уже
готовы, поэтому окно свободно.

## Задача 1 — CI на GitHub Actions (0.5–1 день)

Блокер для всего остального: без зелёного CI релиз 0.1 и PR-процесс
не работают.

- [ ] `.github/workflows/ci.yml`:
  - [ ] job `lint`: ruff check + ruff format --check
  - [ ] job `typecheck`: mypy apps packages
  - [ ] job `test`: pytest -q
  - [ ] Python 3.12, кэш зависимостей (uv или pip)
  - [ ] PostgreSQL для интеграционных тестов: service-контейнер
        `postgres:16-alpine` + `POSTUPI_TEST_DATABASE_URL`
  - [ ] Интеграционные тесты: `alembic upgrade head` перед pytest
- [ ] **Починить `test_hse_fresh_ingestion`** (падает на чистом main:
      `_api_level("bachelor")` → ValueError). Либо фикс hse.py, либо
      xfail с issue-ссылкой — до починки CI не зелёный.
- [ ] Проверка: PR-триггер + push в main.

## Задача 2 — Агрегаты 2026 (0.5 дня)

Публичный артефакт для сайта-витрины и калькулятора. Без кодов и ФИО.

- [ ] Скрипт `apps/tools/export_aggregates.py` (или make-цель):
  - [ ] Для каждого вуза: последний valid snapshot каждой группы
  - [ ] Поля: university, group_title, condition, seats, applications_count,
        min_enrolled_score (по зачисленным/согласиям где есть),
        last_rank, snapshot_date, source_format
  - [ ] Запись в `data/aggregates_2026.csv` (UTF-8, запятые)
  - [ ] Прогон после каждого архива-прогона (A/B/C) — обновление файла
- [ ] Тест-смоук: файл парсится, колонки на месте, нет HMAC/кодов.

## Задача 3 — Шаблоны Issues (0.5 дня)

- [ ] `.github/ISSUE_TEMPLATE/bug_report.md` — баг: шаги, ожидаемое,
      фактическое, вуз/источник, конфиденциальность (никаких реальных UID)
- [ ] `.github/ISSUE_TEMPLATE/feature_request.md` — фича
- [ ] `.github/ISSUE_TEMPLATE/add_university.md` — «добавить вуз»: ссылка на
      шаблон аудита `docs/university_audit_report_template.md`, критерии
      включения из `docs/universities.md`
- [ ] `.github/ISSUE_TEMPLATE/source_changed.md` — «источник изменился»:
      вуз, URL, что сломалось, пример ответа источника
- [ ] `.github/ISSUE_TEMPLATE/config.yml` — связка шаблонов

## Задача 4 — Релиз 0.1 (0.5 дня, после зелёного CI)

- [ ] README: обновить статус (12 источников: ИТМО, ВШЭ, МФТИ монитор +
      9 новых в реестре), ссылка на `docs/integration-plan.md`
- [ ] Тег `v0.1.0` на main (после CI-прогона)
- [ ] Релиз-ноут: что умеет, приватность, ограничения (ВШЭ 500,
      МИФИ/МГСУ/МАДИ/ГУУ не подключены, ФинУнивер — одна группа на вуз,
      Сеченовка — только состав), планы (волна 2, калибровка, сайт)
- [ ] `gh release create v0.1.0` + опубликовать

## Задача 5 — Калибровка v0 (после прогона C, 13–16 авг)

Метки 2026 уже частично есть (ИТМО 1293, МГУ 4245, РНИМУ PDF), но
калибровка требует прогнозов, посчитанных ДО приказов — нужны ≥2–3
snapshot'а на группу (прогоны A/B/C).

- [ ] Метки: хэшировать сырые коды ИТМО тем же `POSTUPI_UID_HMAC_SECRET`
      (namespace `itmo:2026:portal-code:v1`) → `orders_itmo_2026_hmac.csv`
- [ ] Прогнозы: включить `POSTUPI_FORECASTING_ENABLED=true`, прогнать
      `forecast_recompute_job` на snapshot'ах прогона B (последний до
      приказов) — для ИТМО
- [ ] Сопоставить: для каждого прогноза (user_target нет — считаем для
      всех кодов из списка) вероятность vs метка «зачислен»
- [ ] Calibration curve (децили вероятности → доля зачисленных), Brier
      score; отдельно по условиям (общий конкурс, квоты, БВИ)
- [ ] Отчёт `docs/calibration-v0.md`: метрики, выводы, список гипотез для
      v1 (перекосы, пороги, интервал ±1.28σ)
- [ ] МГУ/РНИМУ/МИСИС/Станкин: статусы в данных — метки без хэширования
      (HMAC уже в БД) — калибровка по ним после накопления snapshot'ов

## Порядок и зависимости

```
CI (задача 1) ──► Релиз 0.1 (задача 4)
Агрегаты (2) ──► обновляются после прогонов B/C
Issues (3)     — параллельно
Прогон B (9 авг) ──► Прогон C (11–12 авг) ──► Калибровка v0 (5)
```

## Критерии готовности

- [ ] CI зелёный на push + PR (включая починенный HSE-тест)
- [ ] `data/aggregates_2026.csv` опубликован и обновляется скриптом
- [ ] 4 шаблона Issues + config
- [ ] Релиз v0.1.0 на GitHub с релиз-ноутом
- [ ] После 12 авг: калибровка v0 с метриками в `docs/calibration-v0.md`
